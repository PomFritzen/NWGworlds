using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ContinentFieldDestroyer : MonoBehaviour
{
    public bool shouldDestroy;

    // Start is called before the first frame update
    void Start()
    {
        shouldDestroy = false;
    }

}
